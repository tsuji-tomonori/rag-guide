# /goal 用プロンプトテンプレート

`paper-onepage-summary-imagegen-ja` を使って、画面IDだけで1枚完結の論文要約画像を生成するための `/goal` テンプレート。

## 最小プロンプト

```text
/goal $paper-onepage-summary-imagegen-ja を使用して、画面ID {ID} の論文要約を1枚の image_gen 画像にまとめる。

入力:
- 要約Markdown: pdf/{ID}.md
- 原典PDF: pdf/{ID}.pdf
- 上位デザイン規定: レイアウト・デザイン規定.md

出力:
- pdf/{ID}/{ID}_summary.png
- 16:9、1920x1080 PNG

要件:
- 複数スライドではなく、1枚で完結する読み物向け要約画像にする
- タイトルは論文名ではなく、論文全体の要点・結論を明記する
- 論文名、著者/会議、背景、中心主張、手法、実験設定、主要結果、制約または実用上の示唆を1枚に整理する
- pdf/P002/P002_summary.png のデザインを正として、同じ番号付きカードテンプレートに合わせる
- 7個の情報ブロックに分け、長文段落ではなく見出し+短文で読めるようにする
- 左上に大きい要点タイトル、下に論文名と著者/会議を置く
- 左列に「1 背景」「2 中心主張」、中央に最大カード「3 手法図」、右列に「4 実験設定」「5 主要結果」、下部に「6 読み取り方」「7 制約と実用上の示唆」を配置する
- 中央に手法図、右側に結果/KPI/比較表を入れる
- 背景 #F7F6F1、文字 #2B3A4A、アクセント #5E7E96、補助文字 #9AA4AD を基本にする
- 最終画像は必ず built-in image_gen の生成結果から作り、HTML/SVG/CSS/canvas/ブラウザスクリーンショット/ローカル描画ライブラリで代替生成しない
- image_gen の出力ファイルが見つからない場合は、セッションログの image_generation_call.result base64 を抽出して保存する
- 1920x1080 への正規化は image_gen ラスタ出力の後処理としてのみ行う
- 「総評」「査読結果」「総合評価」などのレビュー・評価内容は入れない
- 必要に応じて pdf/{ID}.pdf も確認する
- 生成後に、出力ファイル、1920x1080、1枚完結、P002デザインとの整合、除外内容の不在、読みやすさを検証する
```

## 追加指定つきプロンプト

```text
/goal $paper-onepage-summary-imagegen-ja を使用して、画面ID {ID} の論文要約を1枚の image_gen 画像にまとめる。

追加指定:
- 読者: {例: RAGを学ぶエンジニア}
- 情報量: {例: 1枚で読める中〜高密度}
- 強調: {例: 手法の構造と実験結果}
- 出力名: {例: pdf/{ID}/{ID}_summary.png}
- 除外: {例: 総評、査読結果、総合評価}

入力:
- 要約Markdown: pdf/{ID}.md
- 原典PDF: pdf/{ID}.pdf
- 上位デザイン規定: レイアウト・デザイン規定.md

出力:
- pdf/{ID}/{ID}_summary.png
- 16:9、1920x1080 PNG

要件:
- 複数スライドではなく、1枚で完結する読み物向け要約画像にする
- タイトルは論文名ではなく、論文全体の要点・結論を明記する
- 論文名、著者/会議、背景、中心主張、手法、実験設定、主要結果、制約または実用上の示唆を1枚に整理する
- pdf/P002/P002_summary.png のデザインを正として、同じ番号付きカードテンプレートに合わせる
- 7個の情報ブロックに分け、長文段落ではなく見出し+短文で読めるようにする
- 左上に大きい要点タイトル、下に論文名と著者/会議を置く
- 左列に「1 背景」「2 中心主張」、中央に最大カード「3 手法図」、右列に「4 実験設定」「5 主要結果」、下部に「6 読み取り方」「7 制約と実用上の示唆」を配置する
- 中央に手法図、右側に結果/KPI/比較表を入れる
- 背景 #F7F6F1、文字 #2B3A4A、アクセント #5E7E96、補助文字 #9AA4AD を基本にする
- 最終画像は必ず built-in image_gen の生成結果から作り、HTML/SVG/CSS/canvas/ブラウザスクリーンショット/ローカル描画ライブラリで代替生成しない
- image_gen の出力ファイルが見つからない場合は、セッションログの image_generation_call.result base64 を抽出して保存する
- 1920x1080 への正規化は image_gen ラスタ出力の後処理としてのみ行う
- 「総評」「査読結果」「総合評価」などのレビュー・評価内容は入れない
- 必要に応じて pdf/{ID}.pdf も確認する
- 生成後に、出力ファイル、1920x1080、1枚完結、P002デザインとの整合、除外内容の不在、読みやすさを検証する
```

## P001 例

```text
/goal $paper-onepage-summary-imagegen-ja を使用して、画面ID P001 の論文要約を1枚の image_gen 画像にまとめる。

追加指定:
- 読者: RAGを学ぶエンジニア
- 情報量: 1枚で読める中〜高密度
- 強調: DPRの手法構造、BM25との比較、QA性能への効果
- 除外: 総評、査読結果、総合評価

入力:
- 要約Markdown: pdf/P001.md
- 原典PDF: pdf/P001.pdf
- 上位デザイン規定: レイアウト・デザイン規定.md

出力:
- pdf/P001/P001_summary.png
- 16:9、1920x1080 PNG

要件:
- 複数スライドではなく、1枚で完結する読み物向け要約画像にする
- タイトルは論文名ではなく、論文全体の要点・結論を明記する
- 論文名、著者/会議、背景、中心主張、手法、実験設定、主要結果、制約または実用上の示唆を1枚に整理する
- pdf/P002/P002_summary.png のデザインを正として、同じ番号付きカードテンプレートに合わせる
- 7個の情報ブロックに分け、長文段落ではなく見出し+短文で読めるようにする
- 左上に大きい要点タイトル、下に論文名と著者/会議を置く
- 左列に「1 背景」「2 中心主張」、中央に最大カード「3 手法図」、右列に「4 実験設定」「5 主要結果」、下部に「6 読み取り方」「7 制約と実用上の示唆」を配置する
- 中央に手法図、右側に結果/KPI/比較表を入れる
- 背景 #F7F6F1、文字 #2B3A4A、アクセント #5E7E96、補助文字 #9AA4AD を基本にする
- 最終画像は必ず built-in image_gen の生成結果から作り、HTML/SVG/CSS/canvas/ブラウザスクリーンショット/ローカル描画ライブラリで代替生成しない
- image_gen の出力ファイルが見つからない場合は、セッションログの image_generation_call.result base64 を抽出して保存する
- 1920x1080 への正規化は image_gen ラスタ出力の後処理としてのみ行う
- 「総評」「査読結果」「総合評価」などのレビュー・評価内容は入れない
- 必要に応じて pdf/P001.pdf も確認する
- 生成後に、出力ファイル、1920x1080、1枚完結、P002デザインとの整合、除外内容の不在、読みやすさを検証する
```
