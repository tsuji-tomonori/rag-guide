# 26 ツール利用レイヤー

## 組み込み情報

- 出力ファイル: `assets/images/v4/26-tool-use-layer.png`
- 対象Markdown: `docs/9.失敗パターンに応じて高度化する/9.9.Agentic RAG.md`
- 挿入位置: `## 9.9.5 ツール台帳`の説明後
- 代替テキスト: `生成モデルのツール提案と実際の実行の間に台帳、スキーマ検証、認可、人承認、監査を置く構造`

## 生成プロンプト

Use case: scientific-educational
Asset type: 日本語RAG技術ガイド内の安全境界図
Primary request: 生成モデルがツールを提案しても直接実行せず、ツール台帳、引数スキーマ、サーバー側認可、人の承認、実行、監査を順に通す安全なツール利用レイヤーを示す図を作る。
Scene/backdrop: オフホワイト `#F7F6F1`。
Style/medium: 細線のベクトル風セキュリティフロー。盾や鍵は補助アイコンとして控えめに使う。
Composition/framing: 16:9横長、1920×1080。四辺に120px相当の安全余白。左に「LLMのツール提案」。中央に5つのゲートを横並び。右に読み取りツールと書き込みツールを分け、書き込みだけ人承認へ分岐。下部に監査ログ。背景色は画面端まで `#F7F6F1` で連続させ、内側の白いページ・スライド枠、外枠、二重余白を作らない。すべての文字・図形・矢印を四辺の安全余白内に収め、クリッピングしない。右上を含め、ページ番号、スライド番号、通し番号は配置しない（工程を表す意味のある番号は可）。
Color palette: `#F7F6F1`、`#2B3A4A`、`#5E7E96`、`#9AA4AD`、淡いブルーグレーのみ。
Typography: Noto Sans JP相当。
Text (verbatim): タイトル「ツール実行は台帳・認可・承認で生成モデルから分離する」。ゲート「ツール台帳」「引数検証」「サーバー側認可」「人の承認」「実行」。分岐「読み取り」「書き込み」。下部「監査ログ」「利用者・操作・結果」。左「LLMの提案」。
Constraints: LLMの提案と実行権限を明確に分離する。書き込みだけが人承認を通る。日本語を正確に描画する。ロゴと透かしなし。
Avoid: LLMが認可器になる構図、赤い警告、派手な色、グラデーション、強い影、写真、3D。
