# 04 RAGの発展段階

## 組み込み情報

- 出力ファイル: `assets/images/v4/04-naive-advanced-modular-rag.png`
- 対象Markdown: `docs/2.アーキテクチャ/2.6.RAGの発展段階.md`
- 挿入位置: 章冒頭（`## 2.6.1 発展段階の使い方`の前）
- 代替テキスト: `基本的なRAG、高度なRAG、モジュール型RAGの構成と複雑性の比較`

## 生成プロンプト

Use case: scientific-educational
Asset type: 日本語RAG技術ガイド内の比較図
Primary request: 基本的なRAG、高度なRAG、モジュール型RAGの違いを、追加される部品と運用複雑性の増加として比較する図を作る。
Scene/backdrop: オフホワイト `#F7F6F1`。
Style/medium: 3カラムのベクトル風比較インフォグラフィック。控えめなカードと細線。
Composition/framing: 16:9横長、1920×1080。四辺に120px相当の安全余白。左から「基本」「高度」「モジュール型」の3カラム。各カラムに簡略化した処理フローと、向く状況を1行で示す。下部に右へ伸びる「複雑性」の軸。背景色は画面端まで `#F7F6F1` で連続させ、内側の白いページ・スライド枠、外枠、二重余白を作らない。すべての文字・図形・矢印を四辺の安全余白内に収め、クリッピングしない。右上を含め、ページ番号、スライド番号、通し番号は配置しない（工程を表す意味のある番号は可）。
Color palette: `#F7F6F1`、`#2B3A4A`、`#5E7E96`、`#9AA4AD`と淡いブルーグレーだけ。
Typography: Noto Sans JP相当。比較見出しを太字にする。
Text (verbatim): タイトル「RAGは固定構成から必要な部品だけを増やす」。見出し「基本的なRAG」「高度なRAG」「モジュール型RAG」。補助語「検索→生成」「再ランキング・補正」「経路選択・反復」。下部「複雑性と運用責任が増える」。
Constraints: 3カラムの幅を均等にする。後段ほど部品が増えることを視覚化するが、優劣の王冠や順位は使わない。日本語を正確に描画する。ロゴと透かしなし。
Avoid: 成熟度を自動的な優劣として描くこと、原色、グラデーション、強い影、写真、3D、過剰な装飾。
