# 8.6 持続可能性

## 参考論文一覧

1. **[FrugalGPT: How to Use Large Language Models While Reducing Cost and Improving Performance](https://arxiv.org/abs/2305.05176)** — Lingjiao Chen, Matei Zaharia, and James Zou, 2023
   - prompt adaptation、model approximation、cascadeによるcost-quality最適化の基本枠組みとして使う。

2. **[RouteLLM: Learning to Route LLMs with Preference Data](https://arxiv.org/abs/2406.18665)** — Isaac Ong et al., 2024
   - queryごとにstrong/weak modelをroutingし、品質を保ちながら費用を下げる考え方を扱う。

3. **[RECOMP: Improving Retrieval-Augmented LMs with Compression and Selective Augmentation](https://arxiv.org/abs/2310.04408)** — Fangyuan Xu, Weijia Shi, and Eunsol Choi, 2023
   - retrieved contextを圧縮し、不要なaugmentationを省くことでtoken費用を抑える。

4. **[Retrieval Augmented Generation or Long-Context LLMs? A Comprehensive Study and Hybrid Approach](https://arxiv.org/abs/2407.16833)** — Zhuowan Li et al., 2024
   - RAGとlong-contextを品質・資源で比較し、Self-Routeで使い分ける判断材料にする。

1. **[LLMLingua: Compressing Prompts for Accelerated Inference of Large Language Models](https://arxiv.org/abs/2310.05736)** — Huiqiang Jiang et al., 2023
   - prompt token削減の可能性と、圧縮率・品質のtrade-offを説明する。

2. **[RAGGED: Towards Informed Design of Retrieval Augmented Generation Systems](https://arxiv.org/abs/2403.09040)** — Jennifer Hsia et al., 2024
   - contextを増やせば常に品質が上がるわけではないため、無駄なretrieval/rerank/token費用を実験で削る根拠にする。

## 8.6.1. Cost taxonomy
- ingestion、parser/OCR、embedding、index storage、query、rerank、LLM、tool、memory、logを分ける。
- 固定費、変動費、更新時費用、incident時費用を区別する。
- vendor invoiceとapplication traceを対応づけられるtagを設計する。

## 8.6.2. Unit economics
- cost/query、cost/session、cost/answerable query、cost/task successを算出する。
- tenant、feature、model、route、document type、risk tierでsliceする。
- 単純な平均単価だけでなく、p95 costと大量出力を監視する。

## 8.6.3. Batch Layerの費用
- full reindexとincremental update、embedding dimension、複製index、backupを比較する。
- parser変更・embedding変更の再処理費用をrelease計画へ含める。
- 低頻度・大規模corpusと高頻度・hybrid searchでstorage選択を分ける。

## 8.6.4. Retrieval・rerankの費用
- top-k、multi-query、複数KB、rerank候補数を段階的に増やして効果を測る。
- 単純queryは低コストroute、難しいqueryだけ高recall/high-rerank routeへ送る。
- cacheはtenant/ACL/versionをkeyへ含め、漏えいを防ぐ。

## 8.6.5. Context・generationの費用
- instruction、evidence、history、answerへtoken budgetを割り当てる。
- dedup、extractive compression、selective augmentation、short answerを評価する。
- input discountやprompt cacheがある場合も、provider依存を明記する。

## 8.6.6. Model routingとcascade
- small/large model、RAG/long-context、通常/agentic flowのrouterを設計する。
- router誤りで品質や安全性が下がらないfallbackとminimum routeを定める。
- 高リスクqueryは費用よりcontrolを優先する。

## 8.6.8. Budget guardrailと改善サイクル
- tenant/feature別budget、quota、alert、hard limit、approvalを設定する。
- cost anomaly、looping agent、過剰tool call、異常token出力を検知する。
- 品質・latency・costのablationを定期実行し、Pareto frontierで判断する。

## 参考論文一覧

1. **[StreamingQA: A Benchmark for Adaptation to New Knowledge over Time in Question Answering Models](https://arxiv.org/abs/2205.11388)** — Adam Liška et al., 2022
   - 新しい知識を追加したときのQA適応を、単なるingestion件数ではなく回答能力で評価する。

2. **[FreshLLMs: Refreshing Large Language Models with Search Engine Augmentation](https://arxiv.org/abs/2310.03214)** — Tu Vu et al., 2023
   - fast-changing knowledgeやfalse-premise questionを含むfreshness評価と、最新根拠の順序・量を考える。

3. **[It's About Time: Incorporating Temporality in Retrieval Augmented Language Models](https://arxiv.org/abs/2401.13222)** — Anoushka Gade and Jorjeta Jetcheva, 2024
   - query time、published time、effective periodをretrievalへ取り込む設計の参考にする。

4. **[FreshDiskANN: A Fast and Accurate Graph-Based ANN Index for Streaming Similarity Search](https://arxiv.org/abs/2105.09613)** — Aditi Singh et al., 2021
   - 更新を受けるANN indexで、search recall、update性能、保守コストを両立する論点を理解する。

1. **[SPFresh: Incremental In-Place Update for Billion-Scale Vector Search](https://arxiv.org/abs/2410.14452)** — Yuming Xu et al., 2024
   - 大規模vector indexのincremental updateを扱い、full rebuildとのtrade-offを説明する発展資料にする。

2. **[Failing Loudly: An Empirical Study of Methods for Detecting Dataset Shift](https://arxiv.org/abs/1810.11953)** — Stephan Rabanser et al., 2018
   - query/corpus分布のshiftを検知する基礎。ただしshift検知と品質劣化を区別する。

3. **[Hidden Technical Debt in Machine Learning Systems](https://proceedings.neurips.cc/paper/2015/hash/86df7dcfd896fcaf2674f757a2463eba-Abstract.html)** — D. Sculley et al., 2015
   - data dependency、configuration debt、monitoring debtをRAG運用へ対応づけ、変更管理を後付けにしない根拠にする。

## 8.7.1. Lifecycle対象とowner
- source document、parsed block、chunk、embedding、sparse/vector index、cacheを区別する。
- 各artifactのowner、source of truth、更新頻度、削除責任を定義する。
- data change、pipeline change、model change、policy changeを別change typeにする。

## 8.7.2. Identity・version・manifest
- stable document_id、reproducible chunk_id、content hashを設計する。
- source snapshot、parser、chunker、embedding、dimension、metric、metadata schemaをmanifestへ記録する。
- 同じ入力と設定から同じartifactを再構築できるようにする。

## 8.7.3. Ingestion方式
- scheduled batch、incremental sync、CDC/event-driven、manual publishを比較する。
- idempotency、dedup、ordering、late event、dead-letter queue、再処理を設計する。
- 失敗文書を全体成功に混ぜず、partial successを可視化する。

## 8.7.4. Blue/green・canary・rollback
- 新indexを別versionで構築し、golden setとshadow trafficで評価する。
- 切替条件、canary比率、rollback条件、旧index保持期間を決める。
- model/prompt/policyとindexの互換組合せをrelease manifestへ固定する。

## 8.7.5. Freshnessとtemporality
- source updateからsearchableになるまでをingestion lagとして測る。
- published_at、effective_from/to、supersedes、as_of queryを扱う。
- 最新文書が常に正しいとは限らないため、authoritative sourceと有効期間を併用する。

## 8.7.6. Delete・ACL propagation
- 削除、権限剥奪、契約終了をindex、cache、memory、replicaへ伝播する。
- tombstone、hard delete、rebuildの使い分けと完了確認を定める。
- revocation/deletion SLOを安全性要件として扱う。

## 8.7.7. Drift monitoring
- query、corpus、metadata、embedding score、zero result、answerability、citation、feedbackを監視する。
- versionを跨いだscore値を直接比較せず、rank overlapやgolden probesを併用する。
- shift alert後に品質testを実行し、benign changeとharmful driftを分ける。

## 8.7.8. Regression・runbook・postmortem
- parser、chunker、embedding、index、prompt、model変更ごとにablationと回帰testを行う。
- stale answer、missing delete、corrupt index、ingestion backlog、schema mismatchのrunbookを用意する。
- 復旧後に原因、検知遅延、影響範囲、再発防止を記録する。

## 参考論文一覧

1. **[Datasheets for Datasets](https://arxiv.org/abs/1803.09010)** — Timnit Gebru et al., 2018
   - RAG corpusの動機、構成、収集、同意、更新、推奨用途、制約を文書化する基礎にする。

2. **[Model Cards for Model Reporting](https://arxiv.org/abs/1810.03993)** — Margaret Mitchell et al., 2018
   - model単体だけでなく、retriever/index/promptを含むRAG system cardへ拡張して利用範囲と評価結果を開示する。

3. **[Closing the AI Accountability Gap: Defining an End-to-End Framework for Internal Algorithmic Auditing](https://arxiv.org/abs/2001.00973)** — Inioluwa Deborah Raji et al., 2020
   - 開発から運用までの内部監査をprocessとevidenceの集合として設計する。

4. **[Towards Trustworthy Retrieval Augmented Generation for Large Language Models: A Survey](https://arxiv.org/abs/2502.06872)** — Bo Ni et al., 2025
   - reliability、privacy、safety、fairness、explainability、accountabilityをRAG文脈で統合するtaxonomyとして使う。

1. **[On the Dangers of Stochastic Parrots: Can Language Models Be Too Big?](https://doi.org/10.1145/3442188.3445922)** — Emily M. Bender et al., 2021
   - data provenance、環境・社会的コスト、想定外利用を含む広いrisk reviewの背景にする。

2. **[Data Statements for Natural Language Processing: Toward Mitigating System Bias and Enabling Better Science](https://aclanthology.org/Q18-1041/)** — Emily M. Bender and Batya Friedman, 2018
   - 言語、話者、地域、収集条件を記述し、corpus coverageと偏りを把握する。

3. **[Fairness and Abstraction in Sociotechnical Systems](https://doi.org/10.1145/3287560.3287598)** — Andrew D. Selbst et al., 2019
   - 技術componentだけを評価して組織・業務・利用者への影響を見落とす問題を避ける。

## 8.8.1. Governanceの対象と原則
- data、retriever、index、model、prompt、tool、memory、evaluation、operationを対象にする。
- accuracyだけでなくsecurity、privacy、fairness、transparency、accountability、costを扱う。
- 設計controlと組織processの両方を必要条件とする。

## 8.8.2. Stakeholder・RACI・意思決定権
- business owner、document owner、data steward、security、privacy、legal、SRE、model owner、auditorを定義する。
- approve、operate、monitor、investigate、retireの責任を割り当てる。
- 例外承認とrisk acceptanceの署名者・期限を明記する。

## 8.8.3. Source governance
- authoritative/informal/external sourceを分類し、owner、trust、更新頻度、利用可否を持つ。
- connector追加、文書公開、poison疑い、source廃止のapproval flowを定める。
- corpus datasheetへcoverage、known gaps、consent、retention、地域・言語偏りを記録する。

## 8.8.4. Use policyとrisk tier
- impact、reversibility、data sensitivity、automation level、affected populationでtier分類する。
- tierごとにcitation、refusal、verification、human approval、logging、testingを変える。
- 医療・法務・人事・権限変更など、RAGを最終判断に使わない範囲を明記する。

## 8.8.5. 文書化とrelease evidence
- system card、architecture、threat model、evaluation report、known limitations、runbookを整備する。
- model/prompt/index/policy versionとrelease decisionを結び付ける。
- 利用者向け説明と内部技術証跡を分けつつ、矛盾させない。

## 8.8.6. Change managementとrelease gate
- 新source、model、embedding、parser、tool、memory policyをchange typeとして管理する。
- risk assessment、security/privacy review、golden set、canary、rollback準備をgateにする。
- 緊急変更の事後reviewと期限付き例外を定める。

## 8.8.7. Human oversight・escalation・appeal
- 低信頼、矛盾、権限不明、高impact actionを人へescalateする。
- reviewerへ根拠、version、uncertainty、履歴を提示する。
- 利用者が誤り・不適切回答・source問題を報告し、訂正状況を追えるようにする。

## 8.8.8. Fairness・coverage・定期review
- language、region、department、document type、user group別にretrieval/answer/refusalをslice評価する。
- corpusに存在しない声や古いpolicyが結果へ与える影響を確認する。
- KPI達成だけでなく、incident、complaint、残余リスク、廃止判断を定期reviewする。
