# 9.5 Structured Data RAG

## 参考論文一覧

| 優先度 | 論文 | 読む観点 | 本文への反映 |
|---|---|---|---|
| 推奨 | [Spider: A Large-Scale Human-Labeled Dataset for Complex and Cross-Domain Semantic Parsing and Text-to-SQL Task](https://arxiv.org/abs/1809.08887)<br>Tao Yu et al. (2018) | cross-domain text-to-SQLの基準dataset。 | schema generalizationと評価指標の出発点にする。 |
| 必読 | [RAT-SQL: Relation-Aware Schema Encoding and Linking for Text-to-SQL Parsers](https://arxiv.org/abs/1911.04942)<br>Bailin Wang et al. (2019) | schema relationとschema linking。 | table / column選択、query中の表現とschemaの対応を説明する。 |
| 必読 | [PICARD: Parsing Incrementally for Constrained Auto-Regressive Decoding from Language Models](https://arxiv.org/abs/2109.05093)<br>Torsten Scholak et al. (2021) | 生成時の構文制約。 | LLM出力をそのまま実行せず、valid SQLへ制約する考え方に使う。 |
| 必読 | [DIN-SQL: Decomposed In-Context Learning of Text-to-SQL with Self-Correction](https://arxiv.org/abs/2304.11015)<br>Mohammadreza Pourreza & Davood Rafiei (2023) | task decompositionとself-correction。 | schema linking、query classification、SQL生成、修正の段階構成に使う。 |
| 必読 | [Can LLM Already Serve as A Database Interface? A BIg Bench for Large-Scale Database Grounded Text-to-SQLs](https://arxiv.org/abs/2305.03111)<br>Jinyang Li et al. (2023) | BIRD benchmarkと実DB値・効率の課題。 | exact matchだけでなくexecution accuracyと効率を測る根拠にする。 |
| 推奨 | [StructGPT: A General Framework for Large Language Model to Reason over Structured Data](https://arxiv.org/abs/2305.09645)<br>Jinhao Jiang et al. (2023) | 外部interfaceを反復利用するreading-then-reasoning。 | SQL以外のtable / KG / APIをtoolとして扱う一般形に使う。 |
| 補助 | [TAPAS: Weakly Supervised Table Parsing via Pre-training](https://arxiv.org/abs/2004.02349)<br>Jonathan Herzig et al. (2020) | SQLを生成せずtable cellとaggregationを選ぶtable QA。 | 小規模表や静的表に対する別アプローチとして紹介する。 |

## 9.5.1. 文書RAGで扱わないデータ
- 最新値、集計、状態、トランザクション結果をvector化した文書から答えない原則を示す。
- structured sourceを正本とし、LLMはquery生成と説明を担う。

## 9.5.2. 対象sourceの分類
- RDB、warehouse、CSV / table、BI semantic layer、REST / GraphQL API、knowledge graphを分ける。
- read consistency、更新頻度、権限、監査要件を整理する。

## 9.5.3. schema理解とsemantic layer
- business term、table、column、join、unit、time zone、effective dateをmetadata化する。
- ユーザー用語とschemaの対応をschema linkingで解く。

## 9.5.4. text-to-SQL pipeline
- source routing→schema selection→question decomposition→SQL generation→validation→execution→result explanationの順にする。
- 生成SQLと実行結果をtraceへ保存する。

## 9.5.5. SQL安全化
- read-only、allowed tables / columns、row limit、timeout、cost limit、parameterization、tenant predicateを強制する。
- DDL / DML、任意function、外部network accessを禁止または別承認にする。

## 9.5.6. table QAとAPI tool
- SQLが不要な静的表はcell selection / aggregationを検討する。
- 最新状態や業務ロジックはAPI toolを正本として呼ぶ。

## 9.5.7. 結果のgrounding
- 回答内の数値をresult row / cellへ紐づける。
- query実行時刻、data freshness、unit、filter条件を表示する。

## 9.5.8. 失敗とfallback
- ambiguous column、複数解釈、empty result、timeout、権限不足時の応答を定義する。
- 危険または不確実なqueryは実行せず確認要求へ分岐する。
