# 9.8 GraphRAG

## 参考論文一覧

| 優先度 | 論文 | 読む観点 | 本文への反映 |
|---|---|---|---|
| 必読 | [Graph Retrieval-Augmented Generation: A Survey](https://arxiv.org/abs/2408.08921)<br>Boci Peng et al. (2024) | Graph-Based Indexing、Graph-Guided Retrieval、Graph-Enhanced Generationの分類。 | 節全体の骨格にする。 |
| 必読 | [From Local to Global: A Graph RAG Approach to Query-Focused Summarization](https://arxiv.org/abs/2404.16130)<br>Darren Edge et al. (2024) | entity graph、community detection、community summaryによるglobal question対応。 | local / global queryとcommunity summaryの中心文献にする。 |
| 必読 | [HippoRAG: Neurobiologically Inspired Long-Term Memory for Large Language Models](https://arxiv.org/abs/2405.14831)<br>Bernal Jiménez Gutiérrez et al. (2024) | knowledge graphとPersonalized PageRankによるmulti-hop retrieval。 | 関係をたどるsingle-step graph retrievalの例にする。 |
| 推奨 | [G-Retriever: Retrieval-Augmented Generation for Textual Graph Understanding and Question Answering](https://arxiv.org/abs/2402.07630)<br>Xiaoxin He et al. (2024) | textual graphから関連subgraphを取得するGraphQA。 | 既存graphを質問対象にする場合と、文書からgraphを構築する場合を分ける。 |
| 推奨 | [GRAG: Graph Retrieval-Augmented Generation](https://arxiv.org/abs/2405.16506)<br>Yuntong Hu et al. (2024) | networked documentsのtextual subgraph retrieval。 | citation graphやsocial / knowledge graphでのsubgraph取得を説明する。 |
| 推奨 | [LightRAG: Simple and Fast Retrieval-Augmented Generation](https://arxiv.org/abs/2410.05779)<br>Zirui Guo et al. (2024) | graph構造とvector表現、dual-level retrieval、incremental update。 | 低・高レベルの検索と更新運用を扱う実装寄りの例にする。 |

## 9.8.1. GraphRAGが解く問題
- global sensemaking、relation query、multi-hop、依存関係、影響分析を例示する。
- 単純な類似検索で十分なqueryをGraphRAGへ送らない。

## 9.8.2. graphの種類
- knowledge graph、document graph、citation graph、entity-chunk bipartite graph、community hierarchyを区別する。
- 既存graph利用と文書からの自動構築を分ける。

## 9.8.3. Graph-Based Indexing
- entity / relation抽出、entity resolution、chunk provenance、community detection、summary生成を扱う。
- 抽出modelとontologyをversion管理する。

## 9.8.4. query classification
- local entity query、path / relation query、global questionを分類する。
- flat vector、graph local、graph globalのroutingを設計する。

## 9.8.5. Graph-Guided Retrieval
- entity linking、neighbor expansion、path search、Personalized PageRank、subgraph optimizationを比較する。
- hop数、node数、token budgetを制約する。

## 9.8.6. Graph-Enhanced Generation
- node、edge、path、community summaryをevidence setへ変換する。
- 回答claimからgraph elementと元chunkへcitationを張る。

## 9.8.7. hybrid vector + graph
- vectorでseedを探しgraphで展開する方式と、graphで絞りtextを読む方式を比較する。
- RRFやsource priorityでflat結果とgraph結果を統合する。

## 9.8.8. update・quality・ACL
- entity merge / split、relation誤抽出、stale edge、incremental updateを扱う。
- node / edge / community summaryへACLを伝播する。
