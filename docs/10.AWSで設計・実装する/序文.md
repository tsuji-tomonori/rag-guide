# 10. AWSでAdvanced RAGを構成する

本章では、第3章から第9章の処理をAWSサービスへ対応付け、サービス間の接続と責務を示します。
個別組織の要求整理ではなく、Amazon Bedrock Knowledge Basesを中心にした実装パターンを扱います。

構成は、取り込み、検索、生成、評価・運用の四経路に分けます。
各経路を分けると、検索品質、生成品質、権限制御、費用を個別に変更して比較できます。

AWSサービスの機能と制約は変更されるため、実装時はリンク先の公式ドキュメントを確認します。
