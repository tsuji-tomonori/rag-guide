# 2.4 Production Baseline

## この節の役割

- Naive RAG、Advanced RAG、Modular RAGを、単なる流行語ではなく責務・制御点・運用難度の違いとして説明する。
- 本番候補をNaive RAGより堅く、無制限なagentic構成より単純な固定pipelineとして位置付ける。
- 高度化を、観測された失敗に対して必要なmoduleを追加する過程として示す。

## 参考論文一覧

| 優先度 | 論文 | この節で参照する論点 | 本文への反映 |
|---|---|---|---|
| 必読 | [Retrieval-Augmented Generation for Knowledge-Intensive NLP Tasks](https://arxiv.org/abs/2005.11401) — Lewis et al. (2020) | retrieveして生成するRAGの原型 | Naive RAGの最小データフローを示す |
| 必読 | [Retrieval-Augmented Generation for Large Language Models: A Survey](https://arxiv.org/abs/2312.10997) — Gao et al. (2023) | Naive、Advanced、Modular RAGの整理 | 本節の発展段階の基本taxonomy |
| 必読 | [Modular RAG: Transforming RAG Systems into LEGO-like Reconfigurable Frameworks](https://arxiv.org/abs/2407.21059) — Gao et al. (2024) | module、operator、routing、scheduling、fusion、loop | Modular RAGを部品の多さではなく制御構造として説明する |
| 推奨 | [Self-RAG](https://arxiv.org/abs/2310.11511) — Asai et al. (ICLR 2024) | 必要時の検索と自己批評 | adaptive / reflective moduleの代表例 |
| 推奨 | [Corrective Retrieval Augmented Generation](https://arxiv.org/abs/2401.15884) — Yan et al. (2024; CRAG) | retrieval結果の品質判定と補正 | verifier / corrective pathを追加する発展例 |
| 推奨 | [Active Retrieval Augmented Generation](https://arxiv.org/abs/2305.06983) — Jiang et al. (2023; FLARE) | 生成中に必要性を検出して検索する | 固定retrieve-then-generate以外のloopを示す |
| 推奨 | [ReAct](https://arxiv.org/abs/2210.03629) — Yao et al. (ICLR 2023) | reasoning、action、observationの反復 | Agentic RAGへ進む場合の基本的な制御ループ |

## 2.6.1. 発展段階を使う目的

- 成熟度を「導入した手法の数」ではなく、必要な制御点と運用能力で捉える。
- 各段階の入力、処理、評価、監査、失敗時fallbackを比較する。
- 高度な段階ほど常に優れるわけではないと明記する。
- queryの一部だけを高度なflowへroutingする混合設計を認める。

## 2.6.2. Naive RAG

- `question → retrieve top-k → prompt → LLM → answer` を最小形として示す。
- 学習、demo、狭いPoCで有用であることを認める。
- ACL、version、rerank、citation、refusal、評価、traceが不足しやすいと説明する。
- Naive RAGを本番の完成形として扱わない。

## 2.6.3. Advanced RAG

- query normalization、hybrid retrieval、filter、rerank、dedup、packingを追加する。
- grounded generation、citation、refusal、traceを標準経路にする。
- 各段階を個別に評価できる固定pipelineとして構成する。
- 多くの社内文書QAで、最初の本番候補になる段階とする。

## 2.6.4. Modular RAG

- router、planner、memory、tool、Graph、SQL、multimodal、verifierをmoduleとして示す。
- linear、conditional、branching、loopingのcontrol flowを説明する。
- moduleのinterface、timeout、retry、停止条件、権限を設計対象にする。
- queryごとに必要なmoduleだけを使うことを基本にする。

## 2.6.5. Adaptive / Corrective / Reflective RAG

- 検索が必要か、根拠が十分か、再検索すべきかを判定する構成を示す。
- Self-RAG、CRAG、FLAREを代表例として位置付ける。
- judgeやreflection自体が誤る可能性を説明する。
- loop回数上限、latency budget、fallbackを必須要件とする。

## 2.6.6. Agentic RAGとの境界

- read-onlyな知識参照と、API・DB・業務操作を伴うactionを分ける。
- tool実行には追加の認証、認可、policy、人間承認が必要とする。
- 単純FAQをagent loopへ入れず、必要なqueryだけroutingする。
- 成功条件を回答品質だけでなく、業務task完了と安全性で測る。

## 2.6.7. 段階比較

- component数、latency、cost、非決定性、評価難度、監査難度を比較する。
- NaiveからAdvancedへ進む条件を、本番要件の不足として示す。
- AdvancedからModularへ進む条件を、固定pipelineで解けない失敗として示す。
- 元へ戻せるfallback pathとrelease単位を定義する。

## 2.6.8. 導入原則

- まずAdvanced RAGの固定baselineを作る。
- 失敗データを分類し、1つずつmoduleを追加する。
- module追加前後を同じ評価セットで比較する。
- 改善が確認できなければmoduleを外す。

## この節の役割

- 最初の本番候補を「小さく限定したAdvanced RAG」として具体化する。
- 初期構成に含める要素と、後から失敗パターンに応じて追加する要素を分ける。
- 現行構成に合わせ、`2.7.1 最初の構成`、`2.7.2 失敗パターン駆動`、`2.7.3 導入順序`の三部でまとめる。

## 参考論文一覧

| 優先度 | 論文 | この節で参照する論点 | 本文への反映 |
|---|---|---|---|
| 必読 | [Retrieval-Augmented Generation for Large Language Models: A Survey](https://arxiv.org/abs/2312.10997) — Gao et al. (2023) | Naive / Advanced / Modular RAG | 初期構成をAdvanced RAGに置く根拠 |
| 必読 | [The Probabilistic Relevance Framework: BM25 and Beyond](https://doi.org/10.1561/1500000019) — Robertson & Zaragoza (2009) | lexical retrieval | 型番、条文、固有名詞を落とさないbaseline要素 |
| 必読 | [Dense Passage Retrieval](https://arxiv.org/abs/2004.04906) — Karpukhin et al. (2020) | semantic retrieval | 言い換えや意味類似を補うdense経路 |
| 必読 | [Reciprocal Rank Fusion](https://doi.org/10.1145/1571941.1572114) — Cormack et al. (2009) | sparse / dense rankingの統合 | score scaleへ依存しにくいfusionの初期候補 |
| 推奨 | [Passage Re-ranking with BERT](https://arxiv.org/abs/1901.04085) — Nogueira & Cho (2019) | 二段階検索 | recallとprecisionを分担するbaseline構成 |
| 必読 | [Lost in the Middle](https://arxiv.org/abs/2307.03172) — Liu et al. (TACL 2024) | contextを増やしすぎる問題 | 少数の良質な根拠へ絞る理由 |
| 必読 | [Enabling Large Language Models to Generate Text with Citations](https://arxiv.org/abs/2305.14627) — Gao et al. (EMNLP 2023) | citation quality | citationを初期要件へ含める根拠 |
| 必読 | [Benchmarking Large Language Models in Retrieval-Augmented Generation](https://arxiv.org/abs/2309.01431) — Chen et al. (2023/2024; RGB) | noise robustness、negative rejection、information integration、counterfactual robustness | refusalとnegative caseを初期評価へ入れる |
| 推奨 | [RAGChecker](https://arxiv.org/abs/2408.08067) — Ru et al. (NeurIPS 2024) | retrieval / generationの細粒度診断 | 失敗パターン駆動で改善する評価基盤 |

## 2.7.1. 最初の構成: 小さなAdvanced RAG

- 対象を1製品、1部門、1業務、1〜数種類のauthoritative sourceへ限定する。
- read-only QAを先に成立させ、toolによる更新操作は含めない。
- Offline側に、parser、chunker、metadata / ACL、embedding、BM25 index、vector index、index versionを置く。
- Online側に、auth / ACL、最小限のquery normalization、hybrid retrieval、fusion、reranker、dedup、context packingを置く。
- Generationに、grounded prompt、citation、refusalを含める。
- 運用に、golden set、trace、latency / cost監視、rollbackを含める。
- 「最小」をcomponent数の少なさではなく、本番事故を診断・抑制できる最小要件と定義する。

## 2.7.2. 育て方: 失敗パターン駆動で拡張する

- まずProduction Baselineを固定し、query、retrieved chunks、context、answer、citationを記録する。
- 失敗をRetrieval、Post-retrieval、Generation、Data、Policyへ分類する。
- 一度に複数の高度化手法を入れず、1変更ずつ評価する。
- 改善指標だけでなく、latency、cost、権限、監査への影響を測る。

## 2.7.3. 導入順序の目安

- Stage 0: Toy RAG
- 小規模文書で概念を確認する。
- 学習・demoに限定し、本番候補とは呼ばない。
- Stage 1: Scoped Baseline
- authoritative sourceを絞る。
- parser、chunker、metadata、ACLを整える。
- BM25 + dense、citation、refusal、traceを入れる。
- Stage 2: Production Baseline
- hybrid retrieval、reranker、dedup、packingを安定化する。
- retrieval / generation / end-to-endを分離評価する。
- index lifecycle、rollback、監視、security testを入れる。
- Stage 3: Controlled Optimization
- query rewriting、compression、verificationを評価で追加する。
- quality、latency、costのSLOを定める。
- driftと本番feedbackを監視する。
- Stage 4: Selective Advanced RAG
- multi-hopにはGraph / hierarchical RAGを選ぶ。
- DB / APIにはstructured retrieval、業務操作にはAgentic RAGを選ぶ。
- 図表・画像にはMultimodal RAGを選ぶ。
- 根拠利用行動が弱い場合のみretrieval-aware tuningを検討する。

## 2.7.4. リリース判定

- golden setでbaseline以上の品質を確認する。
- negative queryと権限ケースで安全側の挙動を確認する。
- p95 latency、error rate、cost/queryがSLO内であることを確認する。
- index、prompt、model、policyをversion bundleとして固定する。
- rollback先と切替手順を準備してから公開する。

## 2.7.5. 本節のまとめ

- 最初から高度なRAGを作るのではなく、小さなAdvanced RAGを安定させる。
- 高度化の判断は、手法の人気ではなく観測された失敗から行う。
- 成熟度は、正しい根拠、権限制御、拒否、評価、監査を継続できるかで測る。
