# 2.3 Post-retrieval

## この節の役割

- Retrievalが返した候補を、LLMが安全かつ効率的に使えるevidence setへ変換する工程を定義する。
- reranking、filtering、deduplication、diversity、compression、context packingの順序と役割を示す。
- 「上位k件をそのままpromptへ貼る」構成の問題点を説明する。

## 参考論文一覧

| 優先度 | 論文 | この節で参照する論点 | 本文への反映 |
|---|---|---|---|
| 必読 | [Passage Re-ranking with BERT](https://arxiv.org/abs/1901.04085) — Nogueira & Cho (2019) | queryと候補を同時入力するcross-encoder reranking | first-stage retrievalとsecond-stage rerankingを分ける根拠 |
| 推奨 | [Document Ranking with a Pretrained Sequence-to-Sequence Model](https://cs.uwaterloo.ca/~jimmylin/publications/Nogueira_etal_FindingsEMNLP2020.pdf) — Nogueira et al. (2020; monoT5) | sequence-to-sequence modelをrankingへ使う | rerankerの方式がcross-encoder分類だけではないことを示す |
| 推奨 | [Zero-Shot Listwise Document Reranking with a Large Language Model](https://arxiv.org/abs/2305.02156) — Ma et al. (2023) | LLMによるlistwise reranking | 精度、cost、再現性、prompt依存のトレードオフを説明する |
| 必読 | [The Use of MMR, Diversity-Based Reranking for Reordering Documents and Producing Summaries](https://doi.org/10.1145/290941.291025) — Carbonell & Goldstein (1998) | relevanceとnoveltyの両立 | 同一文書や近似chunkへの偏りを抑える設計 |
| 必読 | [RECOMP: Improving Retrieval-Augmented LMs with Compression and Selective Augmentation](https://arxiv.org/abs/2310.04408) — Xu et al. (ICLR 2024) | retrieved contextの圧縮と選択的augmentation | context compressionは情報損失を評価しながら使うと説明する |
| 推奨 | [LLMLingua](https://arxiv.org/abs/2310.05736) — Jiang et al. (2023) | prompt compressionによるtoken削減 | 圧縮をlatency / cost最適化の選択肢として位置付ける |
| 必読 | [Lost in the Middle: How Language Models Use Long Contexts](https://arxiv.org/abs/2307.03172) — Liu et al. (TACL 2024) | 長いcontext内の情報位置と利用性能 | 根拠を多く詰めるだけでは品質が上がらない理由 |

## 2.3.1. Post-retrievalの定義

- 入力をrank付きcandidate set、query、ACL、token budgetとする。
- 出力をcitation可能なevidence setとする。
- 目的を「広く集めた候補から、少数で高品質な根拠を作ること」とする。
- retrieval scoreだけで最終contextを決めないことを示す。

## 2.3.2. Reranking

- cross-encoder、sequence-to-sequence、LLM rerankerを概観する。
- first-stageでrecall、rerankerでprecisionを担う二段階構成を説明する。
- rerank対象件数がlatencyとcostへ直結する。
- reranker scoreも正解確率ではなく、並べ替えの相対指標とする。

## 2.3.3. Filtering

- ACL、effective date、document status、source trust、parse qualityで除外する。
- 古い版と最新版が混在する場合の優先規則を定義する。
- 低品質chunk、空chunk、引用できないsourceを除く。
- filter理由をtraceへ残し、検索漏れと区別できるようにする。

## 2.3.4. DeduplicationとDiversity

- 同一chunk、重複文書、near-duplicateを区別する。
- 同一documentからcontextが埋まる偏りを抑える。
- MMRなどでrelevanceとnoveltyを調整する。
- 多様性を上げすぎて主要根拠が下がる副作用も示す。

## 2.3.5. Context compression

- 抽出型、要約型、token-level圧縮を区別する。
- 圧縮前の原文とcitation対応を保持する。
- 数値、否定、条件、例外、表の列関係が失われやすいことを示す。
- 圧縮導入前後でfaithfulnessとcitation accuracyを評価する。

## 2.3.6. Context packing

- token budget内でevidenceを並べる処理と定義する。
- relevance、論理順、文書順、source priority、conflictを考慮する。
- chunkごとにcitation ID、title、page、section、versionを付ける。
- 根拠を先頭・末尾へ置く工夫だけに頼らず、context自体を短く保つ。

## 2.3.7. Evidence setの契約

- supporting evidence、conflicting evidence、background informationを区別する。
- generatorに渡す本文と、UIへ返すcitation metadataを分ける。
- answerability判定に必要な「根拠量」と「根拠の一貫性」を持たせる。
- 元文書へ戻れるprovenanceを保持する。
