---
title: 8. Converseで回答を生成する
description: Retrieveで取得した根拠セットだけをNova Liteへ渡し、引用ID付きの回答を生成します。
---

import { Aside } from '@astrojs/starlight/components';
import CliStep from '../../components/CliStep.astro';
import Command from '../../components/Command.astro';
import ExpectedResult from '../../components/ExpectedResult.astro';
import Checkpoint from '../../components/Checkpoint.astro';
import BuildProgress from '../../components/BuildProgress.astro';
import ResourceTable from '../../components/ResourceTable.astro';

<BuildProgress current={8} />

<ResourceTable
  title="Converseで使う設定値"
  rows={[
  {
    "resource": "生成モデル",
    "setting": "モデルID",
    "value": "amazon.nova-lite-v1:0",
    "note": "1章で疎通確認したモデルを使用します。"
  },
  {
    "resource": "入力",
    "setting": "メッセージ",
    "value": "質問 + Retrieve結果のEvidence Set",
    "note": "Evidence Setにない情報を使わない制約を含めます。"
  },
  {
    "resource": "推論",
    "setting": "maxTokens / temperature / topP",
    "value": "400 / 0.1 / 0.9",
    "note": "教材用の固定値です。用途に応じて評価します。"
  },
  {
    "resource": "根拠付け",
    "setting": "引用形式",
    "value": "[E1]形式",
    "note": "回答中のcitation IDがEvidence Setに存在するか検査します。"
  },
  {
    "resource": "フォールバック",
    "setting": "根拠不足時の動作",
    "value": "根拠不足です",
    "note": "根拠がないときに推測で補わないための条件です。"
  }
]}
/>


ここでは`RetrieveAndGenerate`を使わず、PDFの4ステップのうち「リトリーバー」と「ジェネレーター」を明示的に分けます。`Retrieve`で返ったコンテキストを確認してから`Converse`へ渡すため、生成前の重複排除、版の確認、根拠不足判定を追加できます。`RetrieveAndGenerate`は、取得・プロンプト拡張・生成を一つのAPIで行う比較対象です。

## 手順 8.1. 根拠セット付きメッセージを作成する

<CliStep number="8.1" title="根拠セット付きメッセージを作成する">

<Command phase="prepare" title="根拠セット付きメッセージを準備">

```bash
export RAG_EVIDENCE="$(cat "$RAG_WORK_DIR/evidence.txt")"

jq -n \
  --arg question "$RAG_QUESTION" \
  --arg evidence "$RAG_EVIDENCE" \
  '[{
    role:"user",
    content:[{
      text:(
        "あなたは根拠に基づいて回答するアシスタントです。\n" +
        "次のEvidence Setだけを使って日本語で簡潔に回答してください。\n" +
        "PDFのRAG 4ステップ（文書の取り込み、自然言語クエリ、類似度検索による関連データの取得、クエリとコンテキストを使ったLLM回答）に沿って説明してください。\n" +
        "Knowledge Basesのチャンク化・埋め込み・ベクトルデータベースへの保存、RetrieveとRetrieveAndGenerateの役割の違いを区別してください。\n" +
        "各主張の末尾に[E1]形式のcitation IDを付けてください。\n" +
        "根拠が不足する場合は、推測せず『根拠不足です』と回答してください。\n\n" +
        "質問:\n" + $question + "\n\n" +
        "Evidence Set:\n" + $evidence
      )
    }]
  }]' | tee "$RAG_WORK_DIR/converse-messages.json"

jq -e --arg question "$RAG_QUESTION" '
  length == 1 and
  .[0].role == "user" and
  (.[0].content | length) == 1 and
  (.[0].content[0].text | contains($question)) and
  (.[0].content[0].text | contains("Evidence Set:")) and
  (.[0].content[0].text | contains("[E1]")) and
  (.[0].content[0].text | contains("source=s3://")) and
  (.[0].content[0].text | contains("根拠不足です"))
' \
  "$RAG_WORK_DIR/converse-messages.json" >/dev/null

printf '[CHECK] message structure: 1 user message\n'
printf '[CHECK] prompt contents: question / constraints / Evidence Set / citation IDs\n'
printf 'message verification: PASS\n'
```

</Command>

<ExpectedResult id="s8-1-message" verification="根拠セット付きメッセージを準備">

次の3行が表示されれば成功です。

```text
[CHECK] message structure: 1 user message
[CHECK] prompt contents: question / constraints / Evidence Set / citation IDs
message verification: PASS
```

- `[CHECK] message structure: 1 user message`が表示される
- `[CHECK] prompt contents: question / constraints / Evidence Set / citation IDs`が表示される
- `message verification: PASS`が表示される

</ExpectedResult>

</CliStep>

## 手順 8.2. Converseを呼び出す

<CliStep number="8.2" title="Converseを呼び出す">

<Command phase="execute" title="Converseを呼び出す">

```bash
aws bedrock-runtime converse \
  --region "$AWS_REGION" \
  --model-id "$RAG_GENERATION_MODEL_ID" \
  --messages "file://$RAG_WORK_DIR/converse-messages.json" \
  --inference-config '{"maxTokens":400,"temperature":0.1,"topP":0.9}' \
  | tee "$RAG_WORK_DIR/converse-output.json"

jq -r '.output.message.content[] | select(.text) | .text' \
  "$RAG_WORK_DIR/converse-output.json" \
  | tee "$RAG_WORK_DIR/answer.txt"

test -s "$RAG_WORK_DIR/answer.txt"
export RAG_ANSWER_BYTES="$(wc -c < "$RAG_WORK_DIR/answer.txt")"
printf '[CHECK] Converse API: COMPLETE\n'
printf '[CHECK] generated answer: PRESENT (%s bytes)\n' "$RAG_ANSWER_BYTES"
printf 'Converse execution verification: PASS\n'
```

</Command>

<ExpectedResult id="s8-2-answer" verification="Converseを呼び出す">

次の3行が表示されればAPI呼び出しは成功です。回答内容とcitationの検査は次の8.3で行います。

```text
[CHECK] Converse API: COMPLETE
[CHECK] generated answer: PRESENT (862 bytes)
Converse execution verification: PASS
```

- `[CHECK] Converse API: COMPLETE`が表示される
- `[CHECK] generated answer: PRESENT`が表示される
- `Converse execution verification: PASS`が表示される

</ExpectedResult>

</CliStep>

## 手順 8.3. 最低限の引用検査を行う

<CliStep number="8.3" title="最低限の引用検査を行う">

<Command phase="verify" title="最低限の引用検査を行う" final>

```bash
test -s "$RAG_WORK_DIR/answer.txt"
grep -Eq 'ナレッジベース|Knowledge Bases' "$RAG_WORK_DIR/answer.txt"
grep -Eq '4[[:space:]]*つのステップ|4[[:space:]]*ステップ' "$RAG_WORK_DIR/answer.txt"
grep -Eq 'チャンク|chunk' "$RAG_WORK_DIR/answer.txt"
grep -Eq '埋め込み|embedding' "$RAG_WORK_DIR/answer.txt"
grep -q 'ベクトルデータベース' "$RAG_WORK_DIR/answer.txt"
grep -Eq '類似度検索|関連データ' "$RAG_WORK_DIR/answer.txt"
grep -Eq '取得|Retrieve' "$RAG_WORK_DIR/answer.txt"
grep -q 'RetrieveAndGenerate' "$RAG_WORK_DIR/answer.txt"
grep -Eq '\[E[0-9]+\]' "$RAG_WORK_DIR/answer.txt"

for citation in $(grep -oE '\[E[0-9]+\]' "$RAG_WORK_DIR/answer.txt" | sort -u); do
  grep -Fq "$citation" "$RAG_WORK_DIR/evidence.txt" || {
    printf 'unknown citation: %s\n' "$citation" >&2
    exit 1
  }
done

export RAG_CITATION_ID_COUNT="$(grep -oE '\[E[0-9]+\]' \
  "$RAG_WORK_DIR/answer.txt" | sort -u | wc -l)"
test "$RAG_CITATION_ID_COUNT" -ge 1

printf '[CHECK] generated answer: NON-EMPTY\n'
printf '[CHECK] required content: RAG 4 steps / Knowledge Bases / Retrieve / RetrieveAndGenerate\n'
printf '[CHECK] citation IDs: %s (>= 1)\n' "$RAG_CITATION_ID_COUNT"
printf '[CHECK] citation references: all present in Evidence Set\n'
printf 'citation validation: PASS\n'
```

</Command>

<ExpectedResult id="s8-3-citation" verification="最低限の引用検査を行う">

次の5行が表示されれば成功です。

```text
[CHECK] generated answer: NON-EMPTY
[CHECK] required content: RAG 4 steps / Knowledge Bases / Retrieve / RetrieveAndGenerate
[CHECK] citation IDs: 2 (>= 1)
[CHECK] citation references: all present in Evidence Set
citation validation: PASS
```

- `[CHECK] generated answer: NON-EMPTY`が表示される
- `[CHECK] required content: RAG 4 steps / Knowledge Bases / Retrieve / RetrieveAndGenerate`が表示される
- `[CHECK] citation IDs:`に`1`以上の件数が表示される
- `[CHECK] citation references: all present in Evidence Set`が表示される
- `citation validation: PASS`が表示される

</ExpectedResult>

<Aside type="note" title="この検査だけでは主張の根拠付けを証明できません">
  citation IDの存在確認とキーワード検査は最低限の構文検査です。合格判定は、(1) PDFの4ステップに沿っている、(2) `Retrieve`と`RetrieveAndGenerate`を混同していない、(3)各claimが参照本文に支持されている、(4)根拠がなければ推測せず「根拠不足です」と答える、の4点で行います。
</Aside>

</CliStep>

<Checkpoint>

- generation modelへEvidence Setだけを渡した
- 回答に期待した事実とcitation IDがある
- すべてのcitation IDがEvidence Set内に存在する
- 検索結果と生成結果を別ファイルへ保存した

</Checkpoint>

[Knowledge Baseで検索と回答生成を一撃実行する →](/rag-guide/09-improve-retrieval/)
