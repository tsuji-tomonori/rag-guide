---
title: 7. Retrieveで検索する
description: ナレッジベースへ意味検索クエリを送り、本文・スコア・ソースURI・メタデータを確認します。
---

import { Aside } from '@astrojs/starlight/components';
import CliStep from '../../components/CliStep.astro';
import Command from '../../components/Command.astro';
import ExpectedResult from '../../components/ExpectedResult.astro';
import Checkpoint from '../../components/Checkpoint.astro';
import BuildProgress from '../../components/BuildProgress.astro';
import ResourceTable from '../../components/ResourceTable.astro';

<BuildProgress current={7} />

<ResourceTable
  title="Retrieveで使う設定値"
  rows={[
  {
    "resource": "検索質問",
    "setting": "テキスト",
    "value": "AWS Well-Architected Frameworkの6本の柱を教えてください。",
    "note": "複数chunkから6本の柱の根拠を取得します。"
  },
  {
    "resource": "検索",
    "setting": "overrideSearchType",
    "value": "SEMANTIC",
    "note": "S3 Vectorsのdense semantic searchを明示します。"
  },
  {
    "resource": "検索",
    "setting": "numberOfResults",
    "value": "5",
    "note": "候補を5件取得し、rankとscoreを比較します。"
  },
  {
    "resource": "結果フィールド",
    "setting": "観測値",
    "value": "text / score / source URI / metadata",
    "note": "生成前に根拠本文と出典を確認します。"
  },
  {
    "resource": "根拠セット",
    "setting": "引用ID",
    "value": "[E1], [E2], ...",
    "note": "本文とS3 source URIを同じblockへ整形します。"
  }
]}
/>


生成モデルを呼ぶ前に、検索結果だけを観察します。適切な根拠が候補に含まれなければ、生成モデルを変更しても根本原因は解消しません。

<CliStep number="7.1" title="意味検索を実行する">

<Command>

```bash
export RAG_QUESTION="AWS Well-Architected Frameworkの6本の柱を教えてください。"

jq -n \
  --arg kbId "$RAG_KB_ID" \
  --arg question "$RAG_QUESTION" \
  '{
    knowledgeBaseId:$kbId,
    retrievalQuery:{text:$question},
    retrievalConfiguration:{
      vectorSearchConfiguration:{
        numberOfResults:5,
        overrideSearchType:"SEMANTIC"
      }
    }
  }' | tee "$RAG_WORK_DIR/retrieve-input.json"

aws bedrock-agent-runtime retrieve \
  --cli-input-json "file://$RAG_WORK_DIR/retrieve-input.json" \
  | tee "$RAG_WORK_DIR/retrieve-output.json"
```

</Command>

</CliStep>

<CliStep number="7.2" title="検索結果を検証する">

<Command>

```bash
jq -r '
  .retrievalResults
  | to_entries[]
  | "rank=\(.key + 1) score=\(.value.score) category=\(.value.metadata.category // "-")\nsource=\(.value.location.s3Location.uri // "-")\ntext=\(.value.content.text)\n"
' "$RAG_WORK_DIR/retrieve-output.json"

jq -e '
  (.retrievalResults | length) > 0 and
  ([.retrievalResults[].content.text] | join("\n") | contains("セキュリティ")) and
  ([.retrievalResults[].content.text] | join("\n") | contains("信頼性")) and
  ([.retrievalResults[].content.text] | join("\n") | contains("持続可能性"))
' "$RAG_WORK_DIR/retrieve-output.json" >/dev/null
```

</Command>

表示される各candidateでは、`rank`が返却順、`score`が同一条件内での関連度比較、`source`が根拠PDFのS3 URI、`text`が実際に取り出されたchunk本文です。まず`text`を読み、質問への根拠が含まれるかを確認します。

<ExpectedResult>

1件以上のcandidateが返り、取得本文全体に`セキュリティ`、`信頼性`、`持続可能性`が含まれます。source URIは`aws-well-architected-framework.pdf`を指し、metadataのcategoryは`framework`です。

</ExpectedResult>

<Aside type="caution" title="スコアへ固定閾値を置かないでください">
  scoreの尺度はmodel、store、query、設定によって変わります。単発の値から「0.7以上なら正しい」と決めず、gold evidenceを持つ複数queryでRecall@kと誤検索を測ります。
</Aside>

</CliStep>

<CliStep number="7.3" title="検索結果を根拠セットへ整形する">

<Command>

```bash
jq -r '
  .retrievalResults
  | to_entries
  | map(
      "[E\(.key + 1)]\n" +
      .value.content.text + "\n" +
      "source=" + (.value.location.s3Location.uri // "unknown")
    )
  | join("\n\n")
' "$RAG_WORK_DIR/retrieve-output.json" \
  | tee "$RAG_WORK_DIR/evidence.txt"

test -s "$RAG_WORK_DIR/evidence.txt"
grep -q '\[E1\]' "$RAG_WORK_DIR/evidence.txt"
grep -q 'source=s3://' "$RAG_WORK_DIR/evidence.txt"
```

</Command>

<ExpectedResult>

各candidateに`E1`、`E2`…のcitation IDが付き、本文とS3 source URIが同じblockに保存されます。

</ExpectedResult>

</CliStep>

<Checkpoint>

- `Retrieve`は1件以上を返した
- 6本の柱を説明する正しい根拠が候補にある
- source URIが`aws-well-architected-framework.pdf`を指す
- `evidence.txt`にcitation ID、本文、source URIがある

</Checkpoint>

[Converseで回答を生成する →](/rag-guide/08-converse/)
