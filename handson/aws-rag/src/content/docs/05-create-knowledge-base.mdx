---
title: 5. ナレッジベースを作成する
description: Titan Text Embeddings V2とS3ベクトルインデックスを接続し、S3データソースを作成します。
---

import { Aside } from '@astrojs/starlight/components';
import CloudShellStep from '../../components/CloudShellStep.astro';
import Command from '../../components/Command.astro';
import ExpectedResult from '../../components/ExpectedResult.astro';
import Checkpoint from '../../components/Checkpoint.astro';
import BuildProgress from '../../components/BuildProgress.astro';
import ResourceTable from '../../components/ResourceTable.astro';

<BuildProgress current={5} />

<ResourceTable
  title="作成するナレッジベースとデータソースの設定値"
  rows={[
  {
    "resource": "ナレッジベース",
    "setting": "名前",
    "value": "rag-handson-kb-${RAG_RUN_ID}",
    "note": "作成後にRAG_KB_IDをstate fileへ保存します。"
  },
  {
    "resource": "ナレッジベース",
    "setting": "種別",
    "value": "VECTOR",
    "note": "dense embeddingを使うKnowledge Baseです。"
  },
  {
    "resource": "埋め込みモデル",
    "setting": "モデル／次元数／型",
    "value": "amazon.titan-embed-text-v2:0 / 1024 / FLOAT32",
    "note": "S3 Vector indexのfloat32 / 1024と一致させます。"
  },
  {
    "resource": "ストレージ",
    "setting": "種別",
    "value": "S3_VECTORS",
    "note": "既に作成したvector bucketとindexへ接続します。"
  },
  {
    "resource": "データソース",
    "setting": "バケット／プレフィックス",
    "value": "RAG_SOURCE_BUCKET / documents/",
    "note": "AWS公式PDFとmetadata sidecarだけを取り込みます。"
  },
  {
    "resource": "データソース",
    "setting": "削除ポリシー",
    "value": "DELETE",
    "note": "Cleanup時に取り込みvectorの削除も要求します。"
  },
  {
    "resource": "チャンク分割",
    "setting": "戦略／maxTokens／overlap",
    "value": "FIXED_SIZE / 300 / 10%",
    "note": "比較を始めるbaselineであり、評価後に見直します。"
  }
]}
/>


Knowledge Baseのembedding設定とindex設定を接続します。両方を`1024 / FLOAT32`へ固定し、作成後にAPIからread-backします。作成されたIDはstate fileへ保存し、CloudShell sessionが切り替わっても同じrunを継続できるようにします。

<CloudShellStep number="5.1" title="ナレッジベースを作成する">

<Command phase="prepare" title="Knowledge Baseの作成設定を生成">

```bash
jq -n \
  --arg name "$RAG_KB_NAME" \
  --arg roleArn "$RAG_ROLE_ARN" \
  --arg region "$AWS_REGION" \
  --arg embedModel "$RAG_EMBED_MODEL_ID" \
  --arg vectorBucketArn "$RAG_VECTOR_BUCKET_ARN" \
  --arg indexArn "$RAG_INDEX_ARN" \
  '{
    name:$name,
    description:"CloudShell RAG hands-on knowledge base",
    roleArn:$roleArn,
    knowledgeBaseConfiguration:{
      type:"VECTOR",
      vectorKnowledgeBaseConfiguration:{
        embeddingModelArn:("arn:aws:bedrock:"+$region+"::foundation-model/"+$embedModel),
        embeddingModelConfiguration:{bedrockEmbeddingModelConfiguration:{dimensions:1024,embeddingDataType:"FLOAT32"}}
      }
    },
    storageConfiguration:{type:"S3_VECTORS",s3VectorsConfiguration:{vectorBucketArn:$vectorBucketArn,indexArn:$indexArn}}
  }' | tee "$RAG_WORK_DIR/create-kb.json"

```

</Command>

<Command phase="execute" title="Knowledge Baseを作成してIDを保存">

```bash
aws bedrock-agent create-knowledge-base \
  --region "$AWS_REGION" \
  --cli-input-json "file://$RAG_WORK_DIR/create-kb.json" \
  | tee "$RAG_WORK_DIR/create-kb-output.json"

export RAG_KB_ID="$(jq -r '.knowledgeBase.knowledgeBaseId' "$RAG_WORK_DIR/create-kb-output.json")"
test "${#RAG_KB_ID}" -eq 10
declare -p RAG_KB_ID >> "$RAG_STATE_FILE"
```

</Command>

<Aside type="tip" title="IAM反映待ちで失敗した場合">
  `Invalid service role`などが返った場合は60秒以内を目安に待ち、roleとpolicyのread-backに成功することを確認してから、この作成コマンドを1回再実行します。異なるroleを作り直す必要はありません。
</Aside>

<Command phase="verify" title="ナレッジベースを検証" final>

```bash
for attempt in {1..20}; do
  RAG_KB_STATUS="$(aws bedrock-agent get-knowledge-base \
    --region "$AWS_REGION" \
    --knowledge-base-id "$RAG_KB_ID" \
    --query 'knowledgeBase.status' \
    --output text)"
  printf 'knowledge base status: %s\n' "$RAG_KB_STATUS"
  case "$RAG_KB_STATUS" in
    ACTIVE) break ;;
    FAILED) aws bedrock-agent get-knowledge-base --region "$AWS_REGION" --knowledge-base-id "$RAG_KB_ID"; exit 1 ;;
  esac
  sleep 10
done

test "$RAG_KB_STATUS" = "ACTIVE"

aws bedrock-agent get-knowledge-base \
  --region "$AWS_REGION" \
  --knowledge-base-id "$RAG_KB_ID" \
  --query 'knowledgeBase.{id:knowledgeBaseId,status:status,role:roleArn,type:storageConfiguration.type,index:storageConfiguration.s3VectorsConfiguration.indexArn,dimensions:knowledgeBaseConfiguration.vectorKnowledgeBaseConfiguration.embeddingModelConfiguration.bedrockEmbeddingModelConfiguration.dimensions}' \
  --output table

printf '[CHECK] knowledge base creation: COMPLETE\n'
printf '[CHECK] knowledge base ID: %s\n' "$RAG_KB_ID"
printf '[CHECK] knowledge base status: %s\n' "$RAG_KB_STATUS"
printf '[CHECK] storage type: S3_VECTORS\n'
printf '[CHECK] embedding dimension: 1024\n'
printf 'knowledge base verification: PASS\n'
```

</Command>

<ExpectedResult id="s5-1-kb" verification="ナレッジベースを検証">

statusは`ACTIVE`、store typeは`S3_VECTORS`、dimensionは`1024`です。role ARNとindex ARNが前ページで作成した値に一致します。

```text
| id         | RQVIZHNFAN |
| status     | ACTIVE     |
| type       | S3_VECTORS |
| dimensions | 1024       |
[CHECK] knowledge base creation: COMPLETE
[CHECK] knowledge base ID: RQVIZHNFAN
[CHECK] knowledge base status: ACTIVE
[CHECK] storage type: S3_VECTORS
[CHECK] embedding dimension: 1024
knowledge base verification: PASS
```

- `[CHECK] knowledge base creation: COMPLETE`が表示される
- statusが`ACTIVE`である
- storage typeが`S3_VECTORS`である
- embedding dimensionが`1024`である
- role ARNとindex ARNがこのrunの値である

</ExpectedResult>

</CloudShellStep>

<CloudShellStep number="5.2" title="S3データソースを作成する">

AWS PDFが説明するRAGの最初のステップでは、データのクリーニング、フォーマット、チャンク化、埋め込み、ベクトルデータベースへの保存を行います。ここでは300 token、10% overlapの固定長chunkingを開始値にします。これは万能な推奨値ではなく、後から検索結果のcoverageとノイズで比較するbaselineです。chunking設定はdata source作成後に変更できません。

<Command phase="prepare" title="S3データソースの作成設定を生成">

```bash
jq -n \
  --arg kbId "$RAG_KB_ID" \
  --arg name "$RAG_DS_NAME" \
  --arg bucketArn "arn:aws:s3:::$RAG_SOURCE_BUCKET" \
  '{
    knowledgeBaseId:$kbId,
    name:$name,
    description:"S3 documents for CloudShell RAG hands-on",
    dataSourceConfiguration:{type:"S3",s3Configuration:{bucketArn:$bucketArn,inclusionPrefixes:["documents/"]}},
    dataDeletionPolicy:"DELETE",
    vectorIngestionConfiguration:{chunkingConfiguration:{chunkingStrategy:"FIXED_SIZE",fixedSizeChunkingConfiguration:{maxTokens:300,overlapPercentage:10}}}
  }' | tee "$RAG_WORK_DIR/create-data-source.json"

```

</Command>

<Command phase="execute" title="S3データソースを作成してIDを保存">

```bash
aws bedrock-agent create-data-source \
  --region "$AWS_REGION" \
  --cli-input-json "file://$RAG_WORK_DIR/create-data-source.json" \
  | tee "$RAG_WORK_DIR/create-data-source-output.json"

export RAG_DS_ID="$(jq -r '.dataSource.dataSourceId' "$RAG_WORK_DIR/create-data-source-output.json")"
test "${#RAG_DS_ID}" -eq 10
declare -p RAG_DS_ID >> "$RAG_STATE_FILE"
```

</Command>

<Command phase="verify" title="データソースを検証" final>

```bash
aws bedrock-agent get-data-source \
  --region "$AWS_REGION" \
  --knowledge-base-id "$RAG_KB_ID" \
  --data-source-id "$RAG_DS_ID" \
  --query 'dataSource.{id:dataSourceId,status:status,type:dataSourceConfiguration.type,bucket:dataSourceConfiguration.s3Configuration.bucketArn,prefixes:dataSourceConfiguration.s3Configuration.inclusionPrefixes,deletion:dataDeletionPolicy,chunking:vectorIngestionConfiguration.chunkingConfiguration}' \
  --output yaml

printf '[CHECK] data source creation: COMPLETE\n'
printf '[CHECK] data source ID: %s\n' "$RAG_DS_ID"
printf '[CHECK] data source status: AVAILABLE\n'
printf '[CHECK] inclusion prefix: documents/\n'
printf '[CHECK] deletion policy: DELETE\n'
printf 'data source verification: PASS\n'
```

</Command>

<ExpectedResult id="s5-2-data-source" verification="データソースを検証">

statusは`AVAILABLE`、typeは`S3`、prefixは`documents/`、deletion policyは`DELETE`、chunkingは`FIXED_SIZE / 300 / 10`です。`RAG_KB_ID`と`RAG_DS_ID`はstate fileへ保存されています。

```yaml
id: H63CPNRSM9
status: AVAILABLE
type: S3
prefixes:
  - documents/
deletion: DELETE
chunking:
  chunkingStrategy: FIXED_SIZE
  maxTokens: 300
  overlapPercentage: 10
[CHECK] data source creation: COMPLETE
[CHECK] data source ID: H63CPNRSM9
[CHECK] data source status: AVAILABLE
[CHECK] inclusion prefix: documents/
[CHECK] deletion policy: DELETE
data source verification: PASS
```

- `[CHECK] data source creation: COMPLETE`が表示される
- data sourceが`AVAILABLE`である
- `documents/`だけを取り込む設定である
- `DELETE`、`FIXED_SIZE / 300 / 10`が一致する
- `RAG_DS_ID`がstate fileへ保存される

</ExpectedResult>

</CloudShellStep>

<Checkpoint>

- Knowledge Baseは`ACTIVE`である
- embeddingとindexはともに1024次元floatである
- data sourceは`AVAILABLE`である
- `RAG_KB_ID`と`RAG_DS_ID`をstate fileへ永続化した
- S3 prefixは`documents/`だけである
- data deletion policyは`DELETE`である

</Checkpoint>

[文書を同期する →](/rag-guide/06-sync-documents/)
