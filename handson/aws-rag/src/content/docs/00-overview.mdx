---
title: 0. 全体像と進め方
description: 構築するRAGの処理境界、完了条件、前提権限を確認します。
sidebar:
  order: 0
---

import { Aside, Steps } from '@astrojs/starlight/components';
import Checkpoint from '../../components/Checkpoint.astro';
import BuildProgress from '../../components/BuildProgress.astro';
import ResourceTable from '../../components/ResourceTable.astro';

<BuildProgress current={0} />


## ゴール

このハンズオンでは、AWS公式の日本語版Well-Architected Framework PDFをAWS CLIから`curl`で取得し、Knowledge Basesでchunk化・embeddingしてS3 Vectorsへ登録します。質問時は`Retrieve`で候補を取得し、取得本文だけをEvidence Setとして`Converse`へ渡します。

<figure class="architecture-figure">
  <img
    src="/rag-guide/images/aws-rag-architecture-overview.png"
    alt="S3 DocumentsからKnowledge Base、S3 Vectors、Retrieve、Evidence Set、Converseを経てAnswerへ進むAWS RAG構成図"
  />
  <figcaption>図はImageGen 2.0で作成した全体構成図です。Offline / Indexing、Online / Retrieval、Generationの境界を示しています。</figcaption>
</figure>

## AWS構成図

このハンズオンで実際に作成するAWSリソースを、AWSの物理構成とRAGの論理構成に分けて示します。物理構成はDraw.ioの編集元をリポジトリに残し、公開ページではSVGとして読み込みます。

<figure class="architecture-figure diagram-figure">
  <img
    src="/rag-guide/images/aws-rag-physical-architecture.svg"
    alt="S3、Bedrock Knowledge Bases、S3 Vectors、Retrieve、ConverseをつないだAWS RAGの物理構成図"
  />
  <figcaption>物理構成：S3の文書をKnowledge Basesで取り込み、S3 Vectorsを検索し、Amazon Bedrock Converseで回答を生成します。</figcaption>
</figure>

<figure class="architecture-figure diagram-figure">
  <img
    src="/rag-guide/images/aws-rag-logical-architecture.svg"
    alt="Batch LayerとReal-time Layer、およびAWSリソースの対応を示すAWS RAGの論理構成図"
  />
  <figcaption>論理構成：Batch LayerとReal-time Layerの各責務を、対応するAWSリソースに結び付けています。</figcaption>
</figure>

<ResourceTable
  title="このハンズオンで固定する基準値"
  rows={[
  {
    "resource": "AWSリージョン",
    "setting": "配置リージョン",
    "value": "ap-northeast-1",
    "note": "S3、S3 Vectors、Bedrockを同一リージョンにそろえます。"
  },
  {
    "resource": "原文書",
    "setting": "検索対象",
    "value": "AWS Well-Architected Framework（日本語PDF）",
    "note": "AWS CLIからcurlで取得し、取得日時とSHA-256を保存します。"
  },
  {
    "resource": "ベクトルストア",
    "setting": "基準構成",
    "value": "S3 Vectors / float32 / 1024 / cosine",
    "note": "Titan Text Embeddings V2のembedding設定と一致させます。"
  },
  {
    "resource": "生成モデル",
    "setting": "モデル",
    "value": "amazon.nova-lite-v1:0",
    "note": "Retrieveで確認したEvidence SetだけをConverseへ渡します。"
  }
]}
/>

<Aside type="note" title="この構成で学べる境界">
  Knowledge Basesは取り込みと検索を担当し、生成はConverseへ分離します。検索結果の`text`、`score`、`source URI`を先に観察できるため、「検索できた」と「正しく回答できた」を混同しません。
</Aside>

<Aside type="note" title="1人1回の費用目安">
  基準ケース（PDF 1回取り込み、Retrieve 5回、Converse 2回、同日中にCleanup）では、東京リージョンの公式Price Listに基づき、合計は約`0.005 USD`（税・為替・割引・再実行を除く）です。内訳と計算式は[ハンズオン費用（1人1回）](/rag-guide/cost/)を参照してください。
</Aside>

## 進め方

<Steps>

1. **準備**：AWS CLIの認証、リージョン、CLI機能、モデルを検証します。
2. **Offline Layer**：文書、vector store、IAM role、Knowledge Base、data sourceを作成して同期します。
3. **Online Layer**：`Retrieve`で検索し、metadata filterと候補数を比較します。
4. **Generation**：取得した根拠だけを`Converse`へ渡し、citation ID付きの回答を生成します。
5. **Cleanup**：作成したリソースを依存順に削除し、残存がないことを確認します。

</Steps>

## 必要な権限

実習者のIAM principalには、少なくとも次の操作を許可する必要があります。組織のSCP、permissions boundary、session policyで拒否されている場合は、管理者へハンズオン用sandbox accountまたはroleを依頼してください。

| 対象 | 主な権限 |
|---|---|
| STS | `sts:GetCallerIdentity` |
| S3 | bucketとobjectの作成・取得・削除 |
| S3 Vectors | vector bucket、policy、indexの作成・参照・削除 |
| Bedrock | model一覧・呼び出し、Knowledge Base・data source・ingestionの管理、`Retrieve` |
| IAM | roleとinline policyの作成・参照・削除、`iam:PassRole` |

<Aside type="caution" title="本番アカウントでは実施しないでください">
  ハンズオンは検証用アカウントで実施してください。作成するKnowledge Base用roleは対象S3 bucket、embedding model、vector indexだけへ権限を限定しますが、実習者自身にはリソース作成権限が必要です。
</Aside>

<Checkpoint>

- 検証用AWSアカウントを使える
- AWS CLIを実行できるターミナルを用意できる
- IAM roleを作成し、Bedrockへpassできる
- 終了後にCleanupまで実行できる時間がある
- 数MB以上のPDF downloadとembeddingに伴う通信・少量の利用料金を許容できる

</Checkpoint>

[AWS CLI実行環境を準備する →](/rag-guide/01-prepare-cli/)
